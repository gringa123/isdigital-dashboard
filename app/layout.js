import './globals.css'
export const metadata = { title: 'isdigital — Dashboard', description: 'Painel operacional de tráfego pago' }
export default function RootLayout({ children }) {
  return (<html lang="pt-BR"><body>{children}</body></html>)
}
